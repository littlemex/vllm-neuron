# Core dependencies with Neuron-compatible constraints
setuptools-scm>=8
urllib3>=2.6.0

# vLLM installation
vllm==0.21.0

# Pin <0.137: 0.137's `_IncludedRouter` breaks prometheus-fastapi-instrumentator
fastapi[standard]>=0.115.0,<0.137

# Force transformers v5 upgrade
transformers>=5.5.1,<6.0.0

# Prometheus metrics
prometheus_client

# NKI CPU simulator dtype support
ml_dtypes

# NIXL KV transfer (disaggregated inference)
nixl

# Neuron runtime required by the model path (nki is imported unconditionally,
# e.g. vllm_neuron/functional/argsort_unstable.py). Install from the Neuron
# pip index (see the setup guide's --extra-index-url). libtorch-neuronx-lite
# requires torch==2.11.*, matching vLLM's torch pin.
nki>=0.5.0
neuronx-cc~=2.26
libtorch-neuronx-lite==2.11.*

# benchmark dependency
psutil
requests
boto3
datasets
lm-eval[api,ifeval]
